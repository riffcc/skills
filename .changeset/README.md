# Changesets

This repo uses `changesets` for versioning and release-note generation.

Current intent:

- track repo-level version changes cleanly
- feed CI/CD release automation
- attach generated skill artifacts and updater packages to releases

The packaging and publishing flow is still being built.
